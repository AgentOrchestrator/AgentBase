# ==========================================
# Agent Orchestrator Environment Variables
# ==========================================
#
# This file documents all environment variables needed across the monorepo.
# Each app (daemon, web, cli) has its own .env file.
#
# For local development, run:
#   1. supabase start
#   2. supabase status  (get the keys below)
#   3. Copy values to create .env files in each app directory
#
# For production/remote Supabase:
#   - Get credentials from https://app.supabase.com/project/_/settings/api
#
# ==========================================

# ------------------------------------------
# Root .env (optional, used by CLI installer only)
# Location: .env (root directory)
# ------------------------------------------
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=your_anon_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here


# ------------------------------------------
# Daemon Configuration
# Location: apps/daemon/.env
# ------------------------------------------
# Supabase Configuration (required)
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=your_anon_key_here

# OpenAI Configuration (optional)
# If not set, daemon will use fallback summarization
# OPENAI_API_KEY=sk-your_openai_api_key_here

# Development Mode (optional, default: false)
# Set to 'true' to use fallback summarization instead of OpenAI API
# DEVELOPMENT=false

# Claude Code Home Directory (optional)
# CLAUDE_CODE_HOME=

# Sync Configuration (optional)
# PERIODIC_SYNC_INTERVAL_MS=600000
# SESSION_LOOKBACK_DAYS=30


# ------------------------------------------
# Web App Configuration
# Location: apps/web/.env.local
# ------------------------------------------
# These MUST be prefixed with NEXT_PUBLIC_ to be accessible in the browser
# NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here


# ==========================================
# Setup Instructions for Coding Agents (Claude Code, Cursor, etc.)
# ==========================================
#
# If you're an AI coding assistant setting up this project:
#
# 1. Check if Supabase is running:
#    - Run: supabase status
#    - If not running, run: supabase start
#
# 2. Get credentials from supabase status output:
#    - Look for: "Publishable key" or "anon key" → SUPABASE_ANON_KEY
#    - Look for: "API URL" → SUPABASE_URL
#
# 3. Create environment files:
#
#    apps/daemon/.env:
#    ```
#    SUPABASE_URL=<from supabase status>
#    SUPABASE_ANON_KEY=<from supabase status>
#    DEVELOPMENT=true
#    ```
#
#    apps/web/.env.local:
#    ```
#    NEXT_PUBLIC_SUPABASE_URL=<from supabase status>
#    NEXT_PUBLIC_SUPABASE_ANON_KEY=<from supabase status>
#    ```
#
# 4. The root .env is optional - only used by CLI installer
#
# 5. For non-interactive setup, use:
#    pnpm install-cli:ci -- --local --skip-openai
#
# ==========================================
